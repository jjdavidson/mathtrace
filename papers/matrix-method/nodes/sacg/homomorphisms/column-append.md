---
id: sacg.homomorphisms.column-append
kind: lemma
title: Column-appending homomorphism
requires:
  - sacg.homomorphisms.criterion
source:
  paper: matrix-method.paper
  section: matrix-method.graph-homomorphisms
---

# Main Content

Let $y_1,\dots,y_r,y_{r+1}\in\mathbb{Z}^m$.
Define standardized abelian Cayley graphs $X$ and $Y$ by $M_X=(y_1\ \cdots\ y_r)$ and $M_Y=(y_1\ \cdots\ y_r\ y_{r+1})$, respectively.
Then the identity map on the standard basis induces a graph homomorphism
\[(M_X)^{\mathrm{SACG}}_X\xrightarrow{\circledcirc} (M_Y)^{\mathrm{SACG}}_Y.\]
Thus, appending an arbitrary column to a Heuberger matrix produces a graph homomorphism from the original graph to the graph associated with the enlarged matrix.

## Proof

Let $H_X$ and $H_Y$ be the subgroups generated by the columns of $M_X$ and $M_Y$, respectively.
Every column of $M_X$ is a column of $M_Y$, so $H_X\subseteq H_Y$.

Let $\tau\colon\mathbb{Z}^m\to\mathbb{Z}^m$ be the identity map.
Then $\tau(H_X)\subseteq H_Y$, and $\tau(e_i)=e_i$ for every $i\in\{1,\dots,m\}$.
Therefore, [[sacg.homomorphisms.criterion]] gives the required graph homomorphism.
